# ../reviews/2026-08-03-audit-v2.47.md
## Part A audit of the v2.47 activity chain, per the v2.48-v2.54 build prompt.
## Result: every item PASS or FIXED. Zero STRUCTURAL findings. Shipped as v2.47.1.

## A1. Release hygiene: PASS
package.json 2.47.1, APP_VERSION 2.47.1, lockstep. CHANGELOG carries the
v2.47.0 entry with real counts and the migrations/0004_chain.sql deploy line, plus the
v2.47.1 audit-fix entry. DEPLOY.md leads with the v2.47 steps. Gates run
fresh for this report: tools/audit.mjs clean; npm test 25 suites green, 334
checks; npm run test:backend 512 passed, 0 failed, 21 suites.

## A2. Schema conformance: one FIXED, rest PASS
- chain_events: PASS with one reality note. activity's primary key is
  bigint generated identity, so activity_id is bigint, not uuid. The spec
  clause that governs is "referencing the activity table's pk"; it does.
  All other columns, the unique (project_id, seq), and the desc index match
  the specification exactly, mirrored in schema.sql section 25 and the
  idempotent fix file, twice-applied proof in-suite.
- RLS and revokes: PASS. Select through is_project_member, direct DML
  revoked from authenticated and anon, guard trigger raises on UPDATE and
  DELETE for every role including the owner (tested at superuser).
- Writer: PASS. AFTER INSERT on activity, BEGIN/EXCEPTION WHEN OTHERS THEN
  RAISE WARNING wrap, pg_advisory_xact_lock(hashtext(project_id)) before
  head read, depth guard so the genesis insert cannot recurse.
- chain_repair: FIXED. It was member-gated; the specification requires
  managers only. The guard now uses is_project_manager (schema.sql line 60
  helper). Two new checks pin both sides: a viewer verifies, a viewer
  cannot repair. This is the v2.47.1 patch.
- verify_project_chain: PASS. Definer, granted to authenticated, walks 0
  to head, returns ok/head/divergence plus the unchained coverage count.
- Genesis: PASS. prev formula sha256('REQPUB-GENESIS:' || project_id)
  verified in-suite against an independent digest; the chain.genesis row
  carries the honesty statement verbatim.
- Backfill: PASS. In the fix file, loops all projects, idempotent by
  construction (chained rows skipped), proven by the twice-applied run.

## A3. Recipe conformance, the critical check: PASS
../VERIFY.md section 8 states the eleven-field frozen order, U+001F
joins, the exact UTC microsecond rendering, jsonb::text for meta, the
null rules, the link formula over concatenated lowercase hex, the genesis
rule, the scope limit, and a worked example whose three hashes were
computed for real. The implementation and the document were written as one
and cross-checked field by field; tests/chain-recipe.test.mjs recomputes
the worked example from scratch on node builtins on every push, so doc and
math cannot drift. The deployed recipe is deterministic: fixed field list,
fixed separators, locale-free to_char rendering, no floats, no JSON
canonicalization in SQL. canonicalJson in core.js is untouched by v2.47
and tests/verify-cli.test.mjs passes unmodified inside the green unit run.

## A4. Behavioral verification: PASS (22 checks in chain.test.mjs)
Concurrency: 20 parallel writes, gapless strictly increasing seq.
Tamper: superuser byte flip reported at the exact divergence seq; restoring
true bytes restores ok. Append-only: UPDATE and DELETE raise for
authenticated and for superuser. RLS: rival org reads zero rows; verify
and repair refuse non-members. Repair: trigger-disabled insert surfaces as
unchained coverage, chain_repair recovers exactly one row, head advances,
verify ok. Backfill idempotence: fix applied twice in-suite. Unconfigured
parity: v2.47 touches no frontend surface beyond the version stamp; the
full 334-check unit battery and 512-check backend battery, which exercise
every existing flow, pass unchanged, and audit confirms markup symmetry.

## A5. Security and boundary checks: PASS
Grep of migrations/0004_chain.sql for token and email: zero occurrences; the two new
RPCs return hashes, sequence numbers, and counts only. No service-role
usage anywhere in v2.47. The v2.34.2 boundary checks in the esign and
update-panel suites pass inside the 512. Em-dash and banned-vocabulary
sweep clean across the fix file, VERIFY.md section 8, and the suites.
Authority capture and the excluded integration: zero occurrences in every
v2.47 artifact, per the owner exclusion in section 1, verified by grep.

## A6. Gate: CONCLUDED
Classification totals: PASS 13, FIXED 1, STRUCTURAL 0. The FIXED item
shipped as v2.47.1 in house style with its own changelog entry and pinned
checks. Mode B audit artifact: reqpub-full-v2.47.1-audited.zip. Owner
deploy note: if v2.47.0 was never run, run migrations/0004_chain.sql from v2.47.1
once; it is the same file with the corrected guard, idempotent either way.

Part A passes. Advancing to Part B, release v2.48, per the advance rule.

# ../releases/v2.48.md - CRYPTOGRAPHIC SEALING

Files added: supabase/migrations/0021_sealing.sql, supabase/functions/seal-receipt/
(index.ts, seallib.mjs, vendored core.js), scripts/generate-seal-keys.mjs,
scripts/timestamp-keys.mjs, reqpub-keys.json (placeholder, replaced at
deploy), tests/backend-e2e/sealing.test.mjs (22), tests/seal-fixture.test.mjs
(6). Files changed: app/js/verifybundle.js (buildReceiptBundle), tools/
reqpub-verify.mjs (receipt mode), tests/verify-cli.test.mjs (+1 receipt
case, now 10), app/js/views-collab.js, app/js/data.js, app/js/main.js,
../VERIFY.md (section 9), ../ARCHITECTURE.md, CHANGELOG.md, DEPLOY.md,
schema.sql (section 26 mirror), package.json and APP_VERSION at 2.48.0.

Exclusion honored: no authority_reference column, no authority input, no
change to sign_request_sign for authority, no authority field in receipts
or contexts. Grep of the whole tree for the excluded integration name:
zero. signer_role is the pre-existing column and is untouched.

Two bugs caught by simulation before ship, both fixed: (1) versionFingerprint
is async; the seal builder awaited it wrongly and every seal would have
reported divergence; the end-to-end sim caught it on the first run. (2)
search_path public excludes pgcrypto on Supabase; carried the v2.47 lesson
into the new functions from the start, confirmed by the twice-applied
backend run.

Unknowns: U2 (pg_cron) is optional here; the lazy retry is the shipped
mechanism and the fix file header says so. U5 (deploy edge functions and
set secrets) is required for this release; the owner confirms it at deploy.
Neither blocks the build.

Gates: sealing backend 22/22 (store, idempotence, unsigned and revoked and
non-member refusals, tsa never-downgrade, RLS, token and email boundary,
public key read). Seal unit 6/6 including vendored-core byte parity and the
divergence abort. CLI no-drift 10/10 including sealed-verify, tamper-fail,
baseline-swap-fail. Full backend 534/0 across 22 suites. Unit 340 across 26
suites. Audit clean. Stamp parity.

No dead functions: every RPC granted is called (receipt_store and
receipt_tsa_update by the edge function, seal_context and receipt_for and
receipts_for_project by the app and the reader), every edge function has a
folder and an invoke site, every data-action has a handler, verified by the
audit gate.

Owner decisions still open, neither blocking: D1 the SPEC.md license line
(v2.53), D2 the evidence-pack meta default (v2.52). Advancing to v2.49,
attachment hashing, per the advance rule.

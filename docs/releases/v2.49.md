# ../releases/v2.49.md - ATTACHMENT HASHING

Files added: supabase/migrations/0022_attachment_hash.sql,
tests/backend-e2e/attachment-hash.test.mjs (34, port 55496),
tests/attach-paste.test.mjs (3). Files changed:
supabase/functions/attachment-upload/index.ts (hashing at upload, verify
and backfill JSON modes, still single-file: the dashboard paste is the file,
verbatim, and the new paste gate pins it), supabase/schema.sql (attachments
column and constraint, attachment_add at 13 parameters with the 12-parameter
signature dropped, sign_request_create and sign_request_sign replaced in
place, section 27 mirror), app/js/data.js (sha256_hex in the select, verify
and backfill helpers), app/js/views-collab.js (digest chip, Verify per
hashed file, one manager backfill line), app/js/main.js (attverify,
atthashall), supabase/functions/seal-receipt/seallib.mjs and index.ts and
dist/index.ts (regenerated) and vendored core.js (resynced),
tests/seal-fixture.test.mjs, tests/cli-redteam.test.mjs, ../VERIFY.md
(section 10), ../ATTACHMENTS.md, CHANGELOG.md, package.json and
APP_VERSION at 2.49.0.

Exclusions honored: no fail-closed acceptance rule, no gate on attachment
state, no rollup, no verdict. The excluded integration name: zero
occurrences repo-wide. POSITIONING.md untouched. pg_cron not required (U2
closed): the backfill is the on-demand paged mechanism, invoked from the
Files list, one audit line per page.

Three defects caught against the real tree, all fixed and pinned:

1. sign_request_sign overwrote evidence wholesale at signing, which would
   have erased attachmentsAtSend right before sealing. The build prompt did
   not name this; the tree did. It merges now; the backend suite proves the
   snapshot survives signing and rides seal_context into the receipt with
   the token and email boundary intact.

2. tests/cli-redteam.test.mjs called the async signReceiptHash without
   await, at both call sites, since v2.48: every bundle it built carried
   the literal text "[object Promise]" as its signature. The
   self-signed-forgery check therefore failed on every run (the CLI never
   reached its trust lines), the other attacks passed against garbage, and
   npm test exited 1 at that suite on the tree exactly as pushed. Verified
   pre-existing on a pristine clone of origin HEAD in this environment.
   This is the unit-job CI red on identical trees. Both calls awaited; all
   eight attacks now run against real Ed25519 signatures; 8 of 8 blocked.

3. seallib buildReceipt line 24 did not await the async sha256Hex, so
   project.nameSha256 has serialized as {} in every receipt since v2.48
   where VERIFY.md section 9 promises the project-name hash. Receipts
   already sealed remain valid: each verifies against its own canonical
   bytes. Receipts sealed from now on carry the real hash. Alongside it,
   buildReceipt now refuses an absent captured fingerprint unless the
   legacy path is named explicitly (legacyNoFingerprint, set by the seal
   function from the stored row), closing the red team's blank-fingerprint
   bypass while keeping the documented legacy behavior reachable on
   purpose. seal-fixture pins both directions.

Gates: attachment-hash backend 34/34 (digest stored and refused when
malformed, verify member scope and honest errors, backfill manager gate
and paging and remainder, set_hash idempotence with the marker written
once and an at-upload digest never overwritten, one audit line per page,
attachmentsAtSend clean-and-hashed-only with exactly three keys and no
token and no email, empty evidence unchanged when nothing qualifies, the
sign-time merge, seal_context carry-through). Attachments regression
18/18, esign 29/29, sealing 22/22, sealing red team 26/26, cli red team
8/8. Full unit 348 across 29 suites, exit 0. Full backend 594 across 24
suites, exit 0. Audit clean: 194 markup actions and 197 handlers with no
orphans, rpc symmetry 53 callsites all defined, edge symmetry 6 of 6,
syntax clean on 19 modules, banned language clean on 34 files, stamp
parity at 2.49.0.

Deploy, in order: (1) supabase/migrations/0022_attachment_hash.sql in the SQL editor,
after migrations/0021_sealing.sql; the precheck names anything missing before creating
anything. (2) Dashboard, attachment-upload, paste
supabase/functions/attachment-upload/index.ts over it, verbatim, Verify
JWT stays OFF. (3) Dashboard, seal-receipt, paste the regenerated
dist/index.ts over it, so new receipts carry the real
nameSha256 and the legacy opt-in. (4) Push the tree, hard refresh, footer
2.49.0. (5) Optional, once: on any project with older files, Files list,
Hash existing files.

CI expectation: after this push the unit job's cli-redteam failure is
gone. If runs still fail in 13 seconds across all jobs, the remaining
cause is environmental and the red-step screenshot from the failure email
still ends it.

Advancing next to v2.50, signed webhooks, per the advance rule, on
approval.

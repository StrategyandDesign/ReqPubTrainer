# RELEASE REPORT v2.54: The capabilities page

One reference opened from Help, finished in five minutes: the obvious,
the mechanisms, and the situations the platform was built for. The reader
is assumed competent; nothing is explained twice; every word is authored
and every claim is gated.

## What shipped

app/js/capabilities.js carries COVERED_THROUGH and thirty-four entries in
three tiers: In plain sight, Under the hood shaped mechanism then
consequence, and Where it earns its keep stated as situations. The
mandated coverage is present by id, from immutable baselines through the
sealed receipts in the v2.48 language exactly, the chain, attachment
hashing, the update dashboard, SME tiers, populate from documents, the
evidence pack, signed webhooks with the billing trigger, the MCP surface,
the published schemas, key transparency, the trust mechanics, and the
honest limit: a fingerprint without a receipt proves content, not signer
or time. Each entry is stamped with the release it arrived in; anchored
entries render a quiet Show me that lights the real control through a
one-step inline spotlight riding the existing walkthrough machinery, and
its Done routes to the clean end path, never writing topic state.

Help gains the pinned link above the shelves, after the Path so the
existing render pin holds, and a full-height caps view with a way back.
The panel's prior states render as before, pinned by test.

tools/capabilities-gate.mjs is a named CI step before the suites. Gate A,
stamp parity: COVERED_THROUGH equals package.json or the build fails, so
every version push forces a conscious registry review. Gate B, reference
integrity: every sinceVersion matches a CHANGELOG heading; every anchor
exists in HELP_ANCHORS and renders. Gate C, copy discipline, mechanical:
title and body caps, no exclamation points, no question marks in bodies,
the condescension and superlative lists banned. Gate D, claims
discipline: the positioning doctrine's never-make phrases grep-banned.
The self-test feeds a violating fixture through the same checker and
asserts every class and nine named checks fire, so the gate cannot rot.

## Gates

Audit clean at 2.54.0. The capabilities gate clean over the shipped
registry: 34 entries, every reference resolving, copy discipline holding.
Unit chain green end to end with the 20 new checks. Backend chain exit 0
across all 27 suites, 678 checks, untouched by this release and run
anyway. One prior pin was honored the honest way: the help suite requires
the Path before the first list item, so the pinned link sits after the
Path and above the shelves, which is what the spec asked for.

## Recorded adaptations

For features that predate the mined CHANGELOG window, sinceVersion names
the release era whose heading exists rather than an archaeological first
commit; gate B enforces that every stamp resolves to a real heading. The
sealing stamp is 2.48.1, the heading under which the v2.48 feature ships
in this changelog.

## Deploy

Frontend push only. No SQL, no function deploys. ci.yml gains the gate
step, self-test first, registry second. Footer 2.54.0; Help carries the
pinned reference.

## Open items carried

The reqpub-keys.json .tsr countersignatures remain ungenerated; due in
Phase C2. The SPEC.md license line awaits owner decision D1.

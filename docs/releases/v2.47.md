# RELEASE 1 REPORT: v2.47.0 THE ACTIVITY CHAIN

Files: supabase/migrations/0004_chain.sql (new), supabase/schema.sql (section 25
mirror), tests/backend-e2e/chain.test.mjs (new, 20 checks),
tests/chain-recipe.test.mjs (new, 3 checks), ../VERIFY.md (section 8),
CHANGELOG.md, DEPLOY.md, package.json and APP_VERSION at 2.47.0.

Recipe frozen from the real activity columns: id, org_id, project_id,
actor, actor_name, action, entity_kind, entity_id, summary, meta::text,
created_at UTC micro. Org-level rows, project_id null, sit outside the
chain by design and the suite pins it.

Deviation from the prompt: one, forced by reality. search_path on the
five chain functions is public, extensions, because Supabase installs
pgcrypto in the extensions schema. The harness mirrors that layout and
refused the omission before it could ship.

Unknowns consumed: none. U1 through U5 remain open for later releases.

Gates: chain suite 20 of 20 including twice-applied fix proof, tamper
detection at the exact seq, repair recovery, and cross-org RLS. Full
backend 510 passed, 0 failed, 21 suites. Unit 334 across 25 suites.
Audit clean. Stamp parity.

Manual steps: run migrations/0004_chain.sql once, push, confirm the footer, spot
check verify_project_chain. Then approve or halt release two, v2.48
sealing, which consumes the key script and unknown U2.
